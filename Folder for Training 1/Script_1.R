# Install the first R package
install.packages("BiocManager")

# Install more packages using BiocManager, which install and manager packages from the Bioconductor project for genomic data analysis
BiocManager::install("DESeq2")